# Plan de implementación por fase

Roadmap de 27 fases del master prompt (sección 95), con estado real a la fecha. "Hecho"
significa que hay código funcionando y (donde aplica) tests — no sólo un scaffold vacío.
Ver `PROGRESS.md` para el registro cronológico de qué se hizo en cada sesión.

| Fase | Objetivo | Estado | Notas |
|---|---|---|---|
| 0 | Auditoría y documentación técnica | **Hecho** | `docs/development/*` (este set de archivos) |
| 1 | Monorepo / arquitectura base | **Hecho** | pnpm + Turborepo, 7 packages, 4 apps scaffoldeadas |
| 2 | Design System + Motion System | **Hecho (genéricos) / Parcial (data-shaped)** | Tokens + `MOTION-SYSTEM.md` completos. Catálogo genérico completo: `Button`, `Card`, `BottomSheet`, `Dialog`, `Toast`, `Checkbox`, `FormField`, `Skeleton`, `EmptyState`, `AnimatedPressable`. Faltan sólo los componentes **atados a datos de producto** que no existen todavía (`ProductRow`, `PriceDisplay`, `PriceSourceBadge`, `ConfidenceBadge`, `StoreCard`, `RouteCard`, `OptimizationCard`, `SavingsBadge`) — se construyen con Fase 8/12/16, no antes, para no adivinar su forma |
| 3 | Navigation shell | **Hecho** | 5 tabs con branding real + ruteo completo por estado (`Stack.Protected`: onboarding → auth → tabs) + rutas empujadas desde el stack raíz (`places`, `route-context`, `legal`). Estructuralmente completo — el contenido de cada tab sigue creciendo con sus fases propias (9 Listas, 17 Mapa, etc.), eso nunca fue parte de "navigation shell" en sí |
| 4 | Auth | **Hecho — verificado con DB real** | `AuthProvider` implementado (`apps/api/lib/auth`), endpoints register/login/logout/me/account. Probado de punta a punta contra Postgres real: registro (`201`), login correcto (`200`) e incorrecto (`401` genérico), `me`, logout revoca la sesión (`401` después). Recuperación de contraseña pendiente (necesita proveedor de email, NO DEFINIDO). Sin pantallas de mobile/web todavía (llegan con Fase 5) |
| 5 | Onboarding + legal + privacidad | **Hecho — verificado con DB real** | `apps/mobile`: onboarding de 3 slides, login/registro reales (checkboxes 18+/T&C/privacidad/marketing, todos con la copy obligatoria de FUNCTIONAL.md, rediseñados con opciones Google/Apple — sección 10, sin credenciales OAuth todavía), pantallas legales nativas, modal de consentimiento de ubicación, ruteo por estado de auth (`Stack.Protected`), token en `expo-secure-store`. `/api/consents` probado contra DB real (`200`) |
| 6 | Lugares guardados | **Hecho — verificado con DB real y en el navegador** | CRUD completo (`GET`/`POST /api/places`, `PATCH`/`DELETE /api/places/:id`), probado en Expo Web de punta a punta (crear con favorito, editar nombre, eliminar). **Bug real encontrado y arreglado (Sesión 15)**: "Mis lugares" no navegaba desde Perfil (`route not handled by any navigator`) — `places/index`/`places/form` compartían `name` de `Stack.Screen` entre dos `Stack.Protected` hermanos, lo que rompía `router.push`. Ver `apps/mobile/app/(setup)/places-form.tsx` (alias sin duplicar `name`) |
| 7 | "¿Por dónde vas a andar hoy?" | **Hecho — verificado con DB real y en el navegador** | `SavedRouteContext` CRUD probado contra Postgres real. `TodayRouteContext` (recorrido efímero, sólo cliente): cargar preset guardado, elegir lugares guardados, agregar lugar temporal (geocoded) con el prompt "¿guardar para otra vez?". **Editable (Sesión 15)**: sección nueva "Tu ruta de hoy" lista todos los waypoints (guardados, temporales o ubicación actual) con un botón X para sacar cualquiera — antes, un lugar temporal mal puesto (dirección equivocada) no se podía sacar sin cerrar todo y empezar de nuevo. Probado en vivo: agregar, sacar el mal puesto, el resumen se actualiza bien |
| 8 | Productos y búsqueda | **Hecho — verificado con DB real** | `GET /api/products/search?q=` (ILIKE sobre nombre/variante/alias, sección 37) probado contra Postgres real (`leche`, `yerba`). Pantalla de búsqueda en `apps/mobile` (`app/lists/search.tsx`, `app/preferences/product-form.tsx`) — sin `pg_trgm`/full-text todavía, alcanza para el volumen de seed |
| 9 | Listas | **Hecho — verificado con DB real** | `ShoppingList`/`ShoppingListItem` CRUD completo (`/api/lists`, `/api/lists/:id`, `/api/lists/:id/items(/:itemId)`) probado end-to-end (crear lista, agregar ítem, ver resumen). `apps/mobile`: `ListsContext`, tab "Listas" con datos reales, detalle con cantidad/nota/eliminar |
| 10 | Preferencias | **Hecho — verificado con DB real** | `ProductPreference`/`StorePreference` CRUD (`/api/preferences/products`, `/api/preferences/stores`) probado contra Postgres real. Pantalla `app/preferences/index.tsx` en `apps/mobile` (accesible desde Perfil), con sub-formularios de selección de producto/comercio |
| 11 | Comercios y sucursales | **Hecho — verificado con DB real y con feed SEPA real** | CRUD manual en `apps/admin` (`/stores`, `/stores/:id`) + **ingestión automática real desde SEPA Mayorista** (`packages/data-sources/sepa`, `pnpm data:sepa:import --wholesale`) — 3 sucursales reales de Mar del Plata importadas (Makro, Maxiconsumo, Mayorista Yaguar), ver `docs/data/SEPA.md`. Lectura pública (`/api/stores/branches`) para preferencias (Fase 10). SEPA Minorista (Carrefour/Coto/Toledo/DIA/Vea — confirmados en el feed) pendiente de streaming (sección 26). **MFA de administradores pendiente (NO DEFINIDO) — ver DECISIONS.md, no usar con datos reales todavía** |
| 12 | Precios | **Hecho (carga manual + SEPA Mayorista real) / Pendiente (SEPA Minorista)** | `apps/admin/app/prices` para carga manual (con histórico obligatorio). **13.021 precios reales importados de SEPA Mayorista** (`OFFICIAL_SEPA`, `PriceHistory` en updates) — ver `docs/data/IMPORT-PIPELINE.md`. SEPA Minorista (la mayoría del volumen real de mercado) sigue sin conectar — necesita streaming, no ingestión completa en memoria |
| 13 | Sistema comunitario | **Hecho — verificado con DB real** | `@depaso/community` (`PriceConsensusService`) implementa la pseudológica completa de BUSINESS-RULES.md p.7. `POST /api/price-reports` recibe el reporte (independencia + rate limit 10/hora) y corre `recomputeConsensus` (`apps/api/lib/priceConsensus.ts`), que agrupa por ventana de 24h, descarta baja confianza/saltos imposibles, calcula mediana ponderada y aplica la decisión a `Price`+`PriceHistory` (nunca sobreescribe sin histórico) + `UserTrustScore`. Entrada real desde la app: botón "Reportar" en cada ítem del Modo compra (`purchase/index.tsx`) → `report-price.tsx`. **Probado end-to-end contra Postgres real**: 5 usuarios distintos reportaron el mismo precio real de SEPA Mayorista → promovió a `COMMUNITY_CONFIRMED`, quedó el valor viejo en `PriceHistory` con el motivo exacto de la regla 4, y los 5 reportantes subieron de 0.50 a 0.53 de trust score. Pendiente: adjuntar foto de evidencia (opcional según la doc, no bloqueante) |
| 14 | Consensus + Trust | **Hecho — verificado con DB real** | `@depaso/community` (`CommunityTrustService`) corre dentro del mismo `recomputeConsensus`: `CONFIRMED` para reportantes dentro del consenso promovido, `REJECTED` + `CommunityModerationEvent(PRICE_SENT_TO_REVIEW)` para saltos imposibles, sin tocar el score de la minoría de buena fe que no llegó a consenso (regla 7, "reportar distinto no es infracción"). Fórmula exacta marcada NO DEFINIDO en BUSINESS-RULES.md p.7 — valores de paso documentados como punto de partida. Regla obligatoria respetada: el score nunca se expone crudo, sólo el bucket `ConfidenceLevel` |
| 15 | **Optimization Engine** | **Hecho** | `@depaso/optimization` completo — pipeline de filtro por corredor, restricciones obligatorias, combinaciones 1-3 comercios, score de 5 dimensiones, explicación humana. Tests de los Casos A-E (sección 87) pasando |
| 16 | Top 3 | **Hecho — verificado en el navegador de punta a punta** | `POST /api/optimization` autenticado resuelve lista, precios vigentes, sucursales y preferencias desde Postgres; nunca acepta precios manipulables del cliente. `apps/mobile/app/optimization/index.tsx` permite elegir lista, confirma el recorrido y revela BALANCED/FASTEST/CHEAPEST con stagger que respeta Reduce Motion. El score interno no cruza el límite HTTP. **Dos bugs críticos reales encontrados y arreglados probando el flujo completo**: (Sesión 16) el schema Zod de `PlanStop.order` exigía `positive()` pero el motor emite `order: 0` para la primera parada — rechazaba silenciosamente cualquier respuesta 200 válida con un solo comercio (el caso más común). (Sesión 17) el schema de `plans` exigía `.length(3)` pero el motor devuelve `[]` cuando no hay ningún comercio con esos productos dentro del corredor de la ruta — un resultado real y válido, no una falla — y el cliente lo mostraba con el mismo mensaje genérico que un error de red. Cambiado a `.refine((arr) => arr.length === 0 || arr.length === 3)` + mensaje específico en la UI ("No encontramos comercios con estos productos cerca de tu recorrido de hoy"). Ninguno de los dos se detectó antes porque las pruebas previas usaban `curl` (sin pasar por la validación Zod del cliente). Confirmado real en el navegador tras ambos fixes, con un dataset de producto/precio/sucursal verificado contra la base real: Top 3 con 3 planes, count-up del ahorro, banner de aviso legal, Modo compra y mapa con ruta real, todo renderizando |
| 17 | Mapas y rutas | **Hecho — verificado con DB real y en el navegador** | `createMapboxRouteProvider` (`packages/data-sources/mapbox`, Directions API real, misma cuenta que Fase 28) implementa `RouteProvider` — `apps/api/lib/routing.ts` lo elige automáticamente cuando hay `MAPBOX_ACCESS_TOKEN`, con `MockRouteProvider` de fallback para `PUBLIC_TRANSPORT` (Mapbox no tiene perfil de transporte público) y para cuando no hay token. Wireado tanto en `/api/optimization` (el motor ahora puntúa con distancias reales, no sólo línea recta) como en `POST /api/routes/compute` (nuevo, para el mapa). Mapa visual: `apps/mobile/components/RouteMapView` — Mapbox GL JS con SRI real (hashes calculados contra los archivos exactos de la versión pineada); WebView nativa (`react-native-webview`, sin build para web) y `RouteMapView.web.tsx` con `iframe` directo para Expo Web (resolución de plataforma de Metro, no `Platform.select`). Dibuja el polyline real del `RouteProvider` o, sin token, una línea recta honesta entre paradas (mismo criterio que el fallback de precios/ahorros: nunca inventar). **Probado real con el token del usuario ya cargado**: `/api/config/mapbox-token` devolvió el token real, `/api/routes/compute` devolvió distancia/polyline reales (1687 m por calle vs. 1158 m en línea recta del fallback) — y en el navegador (Expo Web) el mapa **renderizó de verdad**: calles reales de Mar del Plata (San Juan, 14 de Julio, España, Bolívar, Garay, Corrientes), los 2 marcadores numerados en la posición correcta, watermark/atribución de Mapbox visible, controles de zoom funcionando. Único límite que queda: no se probó en dispositivo/emulador nativo (no disponible en este entorno) |
| 18 | Modo compra | **Hecho (sesión actual; pendiente validación manual en dispositivo)** | `apps/mobile/app/purchase/index.tsx`: el usuario elige un plan del Top 3 y recibe un checklist agrupado por comercio, con progreso, checkbox animado+haptic y finalización efímera. No altera la lista fuente ni simula persistencia hasta definir el historial de compras. |
| 19 | Perfil y privacidad | **Hecho — verificado con DB real** | `apps/mobile/app/privacy/index.tsx` ("Configuración → Privacidad y datos", LEGAL.md p.14) con los 4 derechos: **Descargar mis datos** (`GET /api/account/export`, comparte un JSON con cuenta/consentimientos/lugares/listas/preferencias/reportes/trust score vía `Share.share`), **Corregir mis datos** (edita `displayName` con `PATCH /api/auth/me`, ya existía el backend), **Borrar lugares** (borra todos los `UserPlace` con confirmación), **Eliminar cuenta** (`DELETE /api/auth/account`, ya existía — soft delete + anonimiza + revoca sesiones). Los 4 se ejecutan al toque (auto-servicio, sin cola de soporte) — probado real: export con datos reales, rectificación, y eliminación con revocación de sesión confirmada (`401` inmediato después) |
| 20 | Admin | **Hecho (usuarios, reportes+moderación, optimization runs) / NO DEFINIDO (configuración)** | Se agregaron 3 secciones nuevas a `apps/admin`: **Usuarios** (sólo lectura — sin cambio de rol self-service, evita escalar privilegios sin la MFA pendiente), **Reportes y moderación** (lista `PriceReport` + acción para crear `CommunityModerationEvent` — FLOWS.md "Flujo de moderación y apelación" p.8), **Optimization runs** (observabilidad). De paso se encontró y cerró un gap real: `POST /api/optimization` calculaba el Top 3 pero nunca persistía `OptimizationRun`/`OptimizationPlan` pese a que el schema ya lo modelaba desde Fase 1 — ahora sí, best-effort (no rompe la respuesta al usuario si falla). Las 3 secciones probadas contra Postgres real de punta a punta (lista → optimizar → reportar precio → moderar desde admin). "Configuración" queda **NO DEFINIDO**: ninguna doc especifica qué debería configurar — no se inventó una pantalla con ajustes arbitrarios |
| 21 | Tests | **Parcial (mejor que antes)** | 5 casos del motor de optimización + 20 de `@depaso/community` + 29 de `@depaso/data-sources/sepa` (normalización, checksum GTIN, parsing de unidades, CSV, archivo) cubiertos con Vitest — el matching de productos (`normalizeProductName`, `isValidGtinChecksum`, `extractUnitFromDescription`) sí tenía tests dedicados, la nota anterior estaba desactualizada. **Sesión 19**: `apps/mobile` no tenía ningún test automatizado (sin `vitest.config.ts`, sin script `test` — ni typecheck lo detectaba porque Turborepo omite silenciosamente los paquetes sin ese script); se agregó infraestructura de test (Vitest, sólo `lib/**` puro, sin renderer de RN todavía) + 16 tests de `lib/priceDisclosure.ts` (divulgación obligatoria de precio, BUSINESS-RULES.md p.9) cubriendo los 3 sourceType reales, los 3 buckets de confianza, y `formatRecency` con reloj falso (recién/minutos/horas/días/>30 días/reloj desincronizado). El motor de optimización sigue siendo la lógica de mayor riesgo con cobertura real; `resolveProductVariant` (matching en el import job SEPA) permanece sin test dedicado porque está entrelazado con Prisma — el proyecto no mockea la DB, se verifica manualmente contra Postgres real (13.021 precios importados de verdad, Fase 12) |
| 22 | Performance | **Auditoría inicial hecha, sin carga real para medir** | Sin listas grandes ni datos reales de producción todavía para medir tiempos reales. **Sesión 19**: se auditaron todos los `findMany` de `apps/api` buscando riesgo de crecimiento sin límite — la mayoría ya estaban acotados (por `userId`, o con `take` explícito en los endpoints de admin más nuevos). Se encontraron y cerraron 3 sin límite: `GET /api/admin/stores`, `GET /api/admin/store-branches` y `GET /api/stores/branches` (lectura pública para preferencias) — hoy el volumen es bajo (3 sucursales reales de SEPA Mayorista) pero sin `take` se vuelven un riesgo real en cuanto se conecte SEPA Minorista (Fase 12, pendiente), que es mucho mayor volumen. La consulta caliente del motor de optimización (`POST /api/optimization`) ya resuelve precios en un solo `findMany` por lote, no por producto/sucursal (sin N+1) |
| 23 | Motion polish | **Hecho (los momentos previstos) / Pendiente (shared transitions, no forzado)** | 6 de 7 "momentos clave" de `MOTION-SYSTEM.md` implementados y verificados: stagger de entrada en Home, fade+spring al agregar/quitar producto de una lista, indicador pulsante "Analizando..." durante la optimización, reveal del Top 3, count-up del ahorro (`lib/useCountUp.ts`), dibujo progresivo de la ruta en el mapa (`lib/map/buildRouteMapHtml.ts`) y checkbox+haptic del Modo compra. Todos respetan `useReducedMotion()`. Verificado con typecheck+test 21/21 y en el navegador de punta a punta (Home → Top 3 → Modo compra → Mapa). Shared transitions queda deliberadamente sin forzar (la propia doc lo marca "cuando sea técnicamente estable") |
| 24 | Accessibility | **Parcial (mejor que antes)** | Touch targets ≥44px. **Sesión 16**: violación real de contraste (BRAND.md p.12, `brand.route` #6290C3 como texto chico, ≈3.3:1) cerrada en 12 lugares. **Sesión 19**: auditoría programática de todos los `Pressable`/`TouchableOpacity` de `apps/mobile` buscando controles sin `accessibilityLabel` — casi todos ya lo tenían; se encontraron y cerraron 2 gaps reales: (1) el `Pressable` interno de `BottomSheet` que sólo frena la propagación del tap se enfocaba como control vacío para un lector de pantalla antes de llegar al contenido real — arreglado con `accessible={false}`; (2) los toasts (`components/Toast.tsx`) aparecen fuera del flujo de foco actual y un lector de pantalla nunca se enteraba de que aparecieron — arreglado con `AccessibilityInfo.announceForAccessibility()` al mostrarse. Se confirmó por código (no sólo se asumió) que `allowFontScaling` nunca se deshabilita en ningún lado, así que dynamic type del sistema no está bloqueado. Sigue sin auditoría con un lector de pantalla real (no disponible en este entorno) |
| 25 | QA general | **Parcial** | Pasadas reales de QA funcional en el navegador (Sesiones 15-19), no sólo lectura de código: lugares guardados, recorrido de hoy editable, búsqueda de productos, Top 3, Modo compra, mapa, Preferencias, Actividad y Perfil. Encontró y cerró bugs reales en cada pasada (ver notas de Fase 6/7/16), incluido un mensaje de error que escondía una falla de red real detrás del mismo texto que "dirección no encontrada" (Sesión 18) y 2 gaps de accesibilidad reales (Sesión 19, ver Fase 24). Falta: dispositivo/emulador nativo real (gestos, haptics, tamaños reales — no disponible en este entorno) |
| 26 | Build Android/iOS | **No empezado** | Sin emulador/Android Studio/Xcode en este entorno. `apps/mobile` sí se vio corriendo de verdad vía **Expo Web** (`npx expo start --web`, sección "Cómo correr y ver el progreso" en `PROGRESS.md`) — onboarding, login, registro con validación real, legal, todo confirmado visualmente. Expo Web no reemplaza probar en un dispositivo/emulador real (gestos táctiles, haptics, `expo-secure-store` nativo, tamaños de pantalla reales) — sigue pendiente |
| 27 | Preparación deploy | **No empezado** | Sin infraestructura de hosting definida |
| 28 | Geocodificación real (servidor) | **Hecho — código listo, falta la key del usuario** | Proveedor elegido: **Mapbox Geocoding API** (100.000 req/mes gratis, sin tarjeta — confirmado real). `packages/data-sources/mapbox` implementa `GeocodingProvider` (interfaz ya existía desde Fase 1); `POST /api/geocode` y `POST /api/geocode/reverse` en `apps/api`, consumidos por `places/form.tsx` y `route-context/index.tsx` en vez de `Location.geocodeAsync` (que no funciona en Expo Web, causa real del error "No pudimos ubicar esa dirección" — confirmado probando el flujo completo). Sin `MAPBOX_ACCESS_TOKEN` configurado, responde `503 GEOCODING_NOT_CONFIGURED` (verificado real), no rompe el resto de la app. **Paso a paso para conseguir y cargar la key: `docs/development/GEOCODING-SETUP.md`** — 10 minutos, gratis, sin tarjeta |

## Cómo continuar (orden recomendado)

**Postgres local ya está resuelto** en esta máquina de desarrollo (PostgreSQL 17 nativo,
rol/base `depaso` — ver `docs/development/DECISIONS.md`, 2026-09-11). `prisma migrate dev`
corrió, el seed corrió, y Fase 4/6/7 se probaron de punta a punta contra la base real
(register → login → me → logout, lugares, contexto de ruta — ver `PROGRESS.md`, Sesión
7). En otra máquina/CI seguí usando `docker compose up -d` normalmente.

1. **Probar desde `apps/mobile` (Expo Web o dispositivo):** registro → onboarding ya
   visto → tabs → Perfil → Mis lugares → Home → elegir recorrido de hoy → guardarlo como
   preset → buscar productos y armar una lista → configurar preferencias — todo el
   backend ya está confirmado (Fase 4-10), esto es la vuelta de UI completa. El modal de
   ubicación y el geocoding real necesitan permisos de verdad (funcionan en Expo Web con
   la geolocalización del navegador; la prueba definitiva sigue siendo un
   dispositivo/emulador, no disponible en este entorno).
2. ~~**Fase 13-14:** `PriceConsensusService` y `CommunityTrustService`~~ — hecho, punta a
   punta (`packages/community` + `POST /api/price-reports` + botón "Reportar" en Modo
   compra), verificado contra Postgres real.
3. ~~**Fase 16/18:** Top 3 animado y Modo compra~~ — hecho, verificado con 21/21 tareas
   typecheck+test en verde y probado en el navegador (Expo Web).
4. ~~**Fase 19-20:** Perfil/privacidad + resto del admin~~ — hecho, verificado punta a
   punta contra Postgres real (los 4 derechos del titular + usuarios/reportes+moderación/
   optimization runs en `apps/admin`).
5. ~~**Fase 17:** Mapas y rutas~~ — hecho, confirmado visualmente con el token real del
   usuario: mapa de Mar del Plata con calles reales, marcadores y ruta calculada por Mapbox
   Directions. Sólo falta probarlo en dispositivo/emulador nativo (no disponible en este
   entorno — la rama WebView del código es la misma, no debería cambiar de comportamiento).
6. **Fase 21-25:** ampliar tests a los servicios nuevos, performance, motion polish,
   accesibilidad formal, QA end-to-end.
7. **Fase 26-27:** requiere un entorno con Android Studio/Xcode (o EAS Build en la nube) y
   una decisión de hosting — ninguno de los dos está disponible en este entorno de
   desarrollo. Antes de desplegar `apps/admin`/`apps/api` de verdad, resolver también la
   brecha de MFA (Fase 11/12, ver DECISIONS.md) y acotar el CORS por reflejo de origen a
   un allowlist real.
