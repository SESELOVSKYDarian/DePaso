import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function MapScreen() {
  return (
    <PlaceholderScreen
      title="Mapa"
      description="Ruta, paradas y desvío adicional del plan elegido van a aparecer acá (Fase 17)."
    />
  );
}
